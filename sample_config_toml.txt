# Orbit Configuration File
# Place this file in your project directory as 'orbit.toml'
# or in your home directory as '~/.orbit/orbit.toml'

[defaults]
# Compression type: "none", "lz4", "zstd", or "zstd:N" where N is 1-22
compress = "zstd:3"

# Chunk size for I/O operations in KB (default: 1024)
chunk_size = 2048

# Number of retry attempts on failure (default: 3)
retry_attempts = 5

# Preserve file metadata (timestamps, permissions)
preserve_metadata = true

# Number of parallel file operations (0 = auto-detect based on CPU cores)
parallel = 4

[exclude]
# Patterns to exclude from copying (glob patterns)
patterns = [
    "*.tmp",
    "*.log",
    ".git/*",
    ".DS_Store",
    "Thumbs.db",
    "*.swp",
    "*.swo",
    "node_modules/*",
    "__pycache__/*",
    "*.pyc",
]

[audit]
# Audit log format: "json" or "csv"
format = "json"

# Path to audit log file (default: orbit_audit.log in current directory)
# Use ~ for home directory
path = "~/.orbit/audit.log"

# Example configurations for different use cases:

# High-speed local copying (no compression, maximum parallelism)
# [defaults]
# compress = "none"
# parallel = 16
# chunk_size = 4096

# Network transfer (maximum compression, resume enabled)
# [defaults]
# compress = "zstd:19"
# retry_attempts = 10
# chunk_size = 512

# Backup scenario (verify everything, preserve metadata)
# [defaults]
# compress = "zstd:9"
# preserve_metadata = true
# verify_checksum = true
